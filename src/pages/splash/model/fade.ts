import { MotionDuration, MotionEasing } from '../../../shared/lib/motion';

/** Splash entrance fade — shared motion tokens (screen appearance band). */
export const SPLASH_FADE_MS = MotionDuration.splashFade;

export const SPLASH_FADE_EASING = MotionEasing.outStandard;
